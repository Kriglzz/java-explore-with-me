CREATE TABLE IF NOT EXISTS hits (
	hit_id INT GENERATED BY DEFAULT VALUE AS IDENTITY NOT NULL,
	app VARCHAR,
	uri VARCHAR,
	ip VARCHAR,
	timestamp TIMESTAMP WITHOUT TIME ZONE,
	CONSTRAINT pk_hit PRIMARY KEY (hit_id)
);