CREATE TABLE IF NOT EXISTS hits (
	hit_id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	app VARCHAR,
	uri VARCHAR,
	ip VARCHAR,
	time_stamp TIMESTAMP WITHOUT TIME ZONE,
	CONSTRAINT pk_hit PRIMARY KEY (hit_id)
);